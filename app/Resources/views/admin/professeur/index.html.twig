{% extends 'base.html.twig' %}

{% block title %}
    Liste des employées
{% endblock %}

{% block body %}
    <h1>Liste des classes de : </h1>

    <table>
        <thead>
        <th>ID</th>
        <th>PRENOM</th>
        <th>NOM</th>

        </thead>
        {% for classe in arrayClasses %}
        <tbody>
        <tr>
            <td>{{ classe.idPersonne }}</td>
            <td>{{ classe.prenom }}</td>
            <td>{{ classe.nom }}</td>

        </tr>
        {% else %}
            <tr><td>Vous n'avez pas encore de classe(s).</td></tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

